---
- hosts: localhost
  gather_facts: no
  connection: ssh
  tasks:
    - command: whoami
      register: whoami
    - command: 'sudo cat /root/.ssh/test_id_rsa'
      register: testrsa
      
    - debug:
        var: whoami.stdout
    - debug:
        var: testrsa.stdout
